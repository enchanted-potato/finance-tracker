---
phase: 12-data-pages
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/src/main.tsx
  - client/src/lib/queryClient.ts
  - client/src/__tests__/test-utils.tsx
  - client/src/__tests__/AccountsPage.test.tsx
  - client/src/__tests__/BalanceEntryForm.test.tsx
  - client/src/__tests__/HistoryTable.test.tsx
  - client/src/__tests__/LiabilitiesPage.test.tsx
  - client/src/__tests__/PensionPage.test.tsx
  - client/src/components/ui/dialog.tsx
  - client/src/components/ui/form.tsx
  - client/src/components/ui/calendar.tsx
  - client/src/components/ui/popover.tsx
  - client/src/components/ui/table.tsx
  - client/src/components/ui/label.tsx
  - client/src/components/ui/select.tsx
autonomous: true
requirements:
  - RDAT-01
  - RDAT-02
  - RDAT-03
  - RDAT-04
  - RDAT-05
  - RDAT-06
  - RDAT-07

must_haves:
  truths:
    - "@tanstack/react-query@5.100.10 and date-fns@2 are installed in client/"
    - "shadcn dialog, form, calendar, popover, table, label, select components exist in client/src/components/ui/"
    - "QueryClientProvider wraps App in client/src/main.tsx"
    - "renderWithQuery() helper exists in client/src/__tests__/test-utils.tsx with retry:false"
    - "All 5 test stub files exist and npx vitest run exits 0 (all stubs pass)"
  artifacts:
    - path: "client/src/lib/queryClient.ts"
      provides: "QueryClient singleton with staleTime:30s, retry:1"
      exports: ["queryClient"]
    - path: "client/src/main.tsx"
      provides: "App wrapped with QueryClientProvider"
      contains: "QueryClientProvider"
    - path: "client/src/__tests__/test-utils.tsx"
      provides: "renderWithQuery helper for unit tests"
      exports: ["renderWithQuery"]
    - path: "client/src/__tests__/AccountsPage.test.tsx"
      provides: "Test stubs covering RDAT-01 behaviors"
    - path: "client/src/__tests__/BalanceEntryForm.test.tsx"
      provides: "Test stubs covering RDAT-02 behaviors"
    - path: "client/src/__tests__/HistoryTable.test.tsx"
      provides: "Test stubs covering RDAT-03 behaviors"
    - path: "client/src/__tests__/LiabilitiesPage.test.tsx"
      provides: "Test stubs covering RDAT-04, RDAT-05 behaviors"
    - path: "client/src/__tests__/PensionPage.test.tsx"
      provides: "Test stubs covering RDAT-06, RDAT-07 behaviors"
  key_links:
    - from: "client/src/main.tsx"
      to: "client/src/lib/queryClient.ts"
      via: "QueryClientProvider client={queryClient} import"
      pattern: "QueryClientProvider"
    - from: "client/src/__tests__/*.test.tsx"
      to: "client/src/__tests__/test-utils.tsx"
      via: "import { renderWithQuery } from './test-utils'"
      pattern: "renderWithQuery"
---

<objective>
Install all dependencies and create all test scaffolding for Phase 12. This is a blocking Wave 0 plan — no page code can start until all packages are installed, shadcn components exist, and test files compile.

Purpose: Every subsequent plan depends on these packages being available and the test infrastructure being in place. Installing deps and writing test stubs first means Wave 1 and 2 plans can run vitest after each commit without hitting missing-package errors.
Output: Installed packages, 7 shadcn UI components, QueryClientProvider wired into main.tsx, queryClient singleton, renderWithQuery helper, and 5 test stub files that vitest collects and passes.
</objective>

<execution_context>
@/Users/kristiakarakatsani/Repos/finance-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/12-data-pages/12-RESEARCH.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/12-data-pages/12-PATTERNS.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-CONTEXT.md

<interfaces>
<!-- Current main.tsx — executor must see this before modifying it -->
From client/src/main.tsx (current state):
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

From client/src/__tests__/setup.ts (existing test infrastructure):
```typescript
// @ts-ignore
globalThis.IS_REACT_ACT_ENVIRONMENT = false;
```

From client/src/__tests__/PrivateRoute.test.tsx (vi.mock hoisting pattern to replicate):
```typescript
import { describe, it, expect, vi } from 'vitest';
vi.mock('@/contexts/AuthContext', () => ({ useAuth: vi.fn() }));
vi.mock('react-router-dom', () => ({ ... }));
import { render, screen } from '@testing-library/react';
```

From client/src/__tests__/AppSidebar.test.tsx (shadcn mock pattern):
```typescript
vi.mock('@/components/ui/sidebar', () => ({
  Sidebar: ({ children }: { children: React.ReactNode }) => <nav>{children}</nav>,
  // ... each export stubbed
}));
```

Axios mock pattern from client/src/__tests__/apiClient.test.ts:
```typescript
const mockAxios = new MockAdapter(apiClient);
beforeEach(() => { mockAxios.reset(); });
afterEach(() => { vi.clearAllMocks(); });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages and shadcn components</name>
  <files>
    client/package.json
    client/package-lock.json
    client/src/components/ui/dialog.tsx
    client/src/components/ui/form.tsx
    client/src/components/ui/calendar.tsx
    client/src/components/ui/popover.tsx
    client/src/components/ui/table.tsx
    client/src/components/ui/label.tsx
    client/src/components/ui/select.tsx
  </files>
  <read_first>
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/package.json (verify current dependencies before installing, confirm sonner already installed)
  </read_first>
  <action>
Run these three commands IN ORDER from the repo root. The order matters: shadcn must run before npm install to let npm resolve react-day-picker peer deps with date-fns.

Step 1 — Add shadcn components (installs react-day-picker@8.10.1 + react-hook-form + @hookform/resolvers automatically):
```bash
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npx shadcn@2.3.0 add dialog form calendar popover table label select
```
If prompted about overwriting existing files, answer "yes". If prompted about shadcn init, answer with defaults (new-york style, dark base, no CSS variables changes — keep existing).

Step 2 — Install TanStack Query:
```bash
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm install @tanstack/react-query@5.100.10
```

Step 3 — Install date-fns v2 (required by react-day-picker@8.10.1; use --legacy-peer-deps because react-day-picker@8 declares react@^18 peer but project uses React 19 — this is safe, it works at runtime):
```bash
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm install date-fns@2 --legacy-peer-deps
```

Do NOT install react-hook-form or @hookform/resolvers separately — shadcn form command installs them.
Do NOT upgrade react-day-picker — shadcn@2.3.0 calendar is written for the v8 API and breaks on v9.
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm ls @tanstack/react-query react-hook-form date-fns 2>/dev/null | grep -E "@tanstack/react-query|react-hook-form|date-fns"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @tanstack/react-query` output contains `@tanstack/react-query@5.100.10`
    - `npm ls react-hook-form` output contains `react-hook-form@`
    - `npm ls date-fns` output contains `date-fns@2.`
    - `ls client/src/components/ui/dialog.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/form.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/calendar.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/popover.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/table.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/label.tsx` exits 0 (file exists)
    - `ls client/src/components/ui/select.tsx` exits 0 (file exists)
  </acceptance_criteria>
  <done>All 3 install commands exit 0. All 7 shadcn component files exist in client/src/components/ui/. Package versions verified via npm ls.</done>
</task>

<task type="auto">
  <name>Task 2: QueryClient singleton, QueryClientProvider in main.tsx, and all test scaffolding</name>
  <files>
    client/src/lib/queryClient.ts
    client/src/main.tsx
    client/src/__tests__/test-utils.tsx
    client/src/__tests__/AccountsPage.test.tsx
    client/src/__tests__/BalanceEntryForm.test.tsx
    client/src/__tests__/HistoryTable.test.tsx
    client/src/__tests__/LiabilitiesPage.test.tsx
    client/src/__tests__/PensionPage.test.tsx
  </files>
  <read_first>
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/main.tsx (read current content before modifying — must wrap App with QueryClientProvider)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/lib/firebase.ts (singleton pattern to mirror for queryClient.ts)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/setup.ts (existing global test setup)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/PrivateRoute.test.tsx (vi.mock hoisting and test structure to replicate)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/AppSidebar.test.tsx (shadcn mock pattern to replicate)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/apiClient.test.ts (MockAdapter pattern to replicate in page tests)
    - /Users/kristiakarakatsani/Repos/finance-tracker/client/src/lib/apiClient.ts (import path used in all page test mocks)
  </read_first>
  <action>
**Step 1 — Create `client/src/lib/queryClient.ts`** (singleton module pattern from firebase.ts):

```typescript
import { QueryClient } from '@tanstack/react-query';

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 30_000,
      retry: 1,
    },
  },
});
```

**Step 2 — Update `client/src/main.tsx`** (wrap App with QueryClientProvider):

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { QueryClientProvider } from '@tanstack/react-query'
import { queryClient } from '@/lib/queryClient'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
    </QueryClientProvider>
  </StrictMode>,
)
```

QueryClientProvider wraps App so ALL routes (including /login) have access to the QueryClient. AuthProvider and BrowserRouter remain inside App.tsx unchanged.

**Step 3 — Create `client/src/__tests__/test-utils.tsx`** (renderWithQuery helper):

```typescript
import React from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { render } from '@testing-library/react';
import type { RenderResult } from '@testing-library/react';

export function renderWithQuery(ui: React.ReactElement): RenderResult {
  const queryClient = new QueryClient({
    defaultOptions: {
      queries: { retry: false },
      mutations: { retry: false },
    },
  });
  return render(
    <QueryClientProvider client={queryClient}>{ui}</QueryClientProvider>
  );
}
```

`retry: false` is critical — prevents Vitest from timing out when a query fails in a test.

**Step 4 — Create `client/src/__tests__/AccountsPage.test.tsx`** (stubs covering RDAT-01 behaviors):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import MockAdapter from 'axios-mock-adapter';

// Mocks BEFORE imports (vi.mock hoisting requirement)
vi.mock('@/components/ui/dialog', () => ({
  Dialog: ({ children, open }: { children: React.ReactNode; open: boolean }) =>
    open ? <div data-testid="dialog">{children}</div> : null,
  DialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/alert-dialog', () => ({
  AlertDialog: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogDescription: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogCancel: ({ children }: { children: React.ReactNode }) => <button>{children}</button>,
  AlertDialogAction: ({ children, onClick }: { children: React.ReactNode; onClick?: () => void }) => <button onClick={onClick}>{children}</button>,
}));

vi.mock('@/components/ui/calendar', () => ({
  Calendar: () => <div data-testid="calendar" />,
}));

vi.mock('@/components/ui/popover', () => ({
  Popover: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/select', () => ({
  Select: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectValue: ({ placeholder }: { placeholder?: string }) => <span>{placeholder}</span>,
  SelectContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectItem: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/table', () => ({
  Table: ({ children }: { children: React.ReactNode }) => <table>{children}</table>,
  TableHeader: ({ children }: { children: React.ReactNode }) => <thead>{children}</thead>,
  TableBody: ({ children }: { children: React.ReactNode }) => <tbody>{children}</tbody>,
  TableHead: ({ children }: { children: React.ReactNode }) => <th>{children}</th>,
  TableRow: ({ children, onClick, className }: { children: React.ReactNode; onClick?: () => void; className?: string }) => <tr onClick={onClick} className={className}>{children}</tr>,
  TableCell: ({ children, className }: { children: React.ReactNode; className?: string }) => <td className={className}>{children}</td>,
}));

import React from 'react';
import { screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { renderWithQuery } from './test-utils';
import { apiClient } from '@/lib/apiClient';
import { AccountsPage } from '@/pages/AccountsPage';

const mockAxios = new MockAdapter(apiClient);

describe('AccountsPage', () => {
  beforeEach(() => {
    mockAxios.reset();
  });
  afterEach(() => {
    vi.clearAllMocks();
  });

  it('renders account type list from API (RDAT-01)', async () => {
    mockAxios.onGet('/api/accounts/types').reply(200, [
      { id: 1, name: 'ISA', is_pension: false, in_use: true },
      { id: 2, name: 'Checking', is_pension: false, in_use: false },
    ]);
    mockAxios.onGet('/api/accounts/history').reply(200, []);
    renderWithQuery(<AccountsPage />);
    await waitFor(() => {
      expect(screen.getByText('ISA')).toBeTruthy();
      expect(screen.getByText('Checking')).toBeTruthy();
    });
  });

  it('shows Add button on the page (RDAT-01)', async () => {
    mockAxios.onGet('/api/accounts/types').reply(200, []);
    mockAxios.onGet('/api/accounts/history').reply(200, []);
    renderWithQuery(<AccountsPage />);
    await waitFor(() => {
      expect(screen.getByRole('button', { name: /add/i })).toBeTruthy();
    });
  });

  it('renders page title "Accounts" (RDAT-01)', async () => {
    mockAxios.onGet('/api/accounts/types').reply(200, []);
    mockAxios.onGet('/api/accounts/history').reply(200, []);
    renderWithQuery(<AccountsPage />);
    await waitFor(() => {
      expect(screen.getByText('Accounts')).toBeTruthy();
    });
  });
});
```

**Step 5 — Create `client/src/__tests__/BalanceEntryForm.test.tsx`** (stubs covering RDAT-02 behaviors):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import MockAdapter from 'axios-mock-adapter';

vi.mock('@/components/ui/calendar', () => ({
  Calendar: () => <div data-testid="calendar" />,
}));

vi.mock('@/components/ui/popover', () => ({
  Popover: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/select', () => ({
  Select: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectValue: ({ placeholder }: { placeholder?: string }) => <span>{placeholder}</span>,
  SelectContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectItem: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

import React from 'react';
import { render, screen } from '@testing-library/react';
import { renderWithQuery } from './test-utils';
import { apiClient } from '@/lib/apiClient';
import { BalanceEntryForm } from '@/components/data/BalanceEntryForm';

const mockAxios = new MockAdapter(apiClient);

const sampleTypes = [{ id: 1, name: 'ISA', is_pension: false, in_use: true }];
const historyQueryKey = ['accounts', 'history'] as const;
const noop = () => Promise.resolve(undefined);

describe('BalanceEntryForm', () => {
  beforeEach(() => { mockAxios.reset(); });
  afterEach(() => { vi.clearAllMocks(); });

  it('renders Save Entry button (RDAT-02)', () => {
    renderWithQuery(
      <BalanceEntryForm
        items={sampleTypes}
        historyQueryKey={historyQueryKey}
        submitEntry={noop}
        itemLabel="account"
      />
    );
    expect(screen.getByRole('button', { name: /save entry/i })).toBeTruthy();
  });

  it('renders date picker trigger button (RDAT-02)', () => {
    renderWithQuery(
      <BalanceEntryForm
        items={sampleTypes}
        historyQueryKey={historyQueryKey}
        submitEntry={noop}
        itemLabel="account"
      />
    );
    // Date picker trigger shows today's date or "Pick a date" placeholder
    expect(screen.getByRole('button', { name: /date|today|jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec/i })).toBeTruthy();
  });
});
```

**Step 6 — Create `client/src/__tests__/HistoryTable.test.tsx`** (stubs covering RDAT-03 behaviors):

```typescript
import { describe, it, expect, vi } from 'vitest';

vi.mock('@/components/ui/table', () => ({
  Table: ({ children }: { children: React.ReactNode }) => <table>{children}</table>,
  TableHeader: ({ children }: { children: React.ReactNode }) => <thead>{children}</thead>,
  TableBody: ({ children }: { children: React.ReactNode }) => <tbody>{children}</tbody>,
  TableHead: ({ children }: { children: React.ReactNode }) => <th>{children}</th>,
  TableRow: ({ children, onClick, className }: { children: React.ReactNode; onClick?: () => void; className?: string }) => <tr onClick={onClick} className={className}>{children}</tr>,
  TableCell: ({ children, className }: { children: React.ReactNode; className?: string }) => <td className={className}>{children}</td>,
}));

import React from 'react';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { HistoryTable } from '@/components/data/HistoryTable';

const sampleData = [
  {
    date: '2025-02-01',
    total: 1500.0,
    entries: [{ entry_id: 1, type_id: 1, type_name: 'Checking', balance: 1500.0 }],
  },
  {
    date: '2025-01-15',
    total: 3000.0,
    entries: [
      { entry_id: 2, type_id: 1, type_name: 'Checking', balance: 1000.0 },
      { entry_id: 3, type_id: 2, type_name: 'ISA', balance: 2000.0 },
    ],
  },
];

describe('HistoryTable', () => {
  it('renders daily total rows from data (RDAT-03)', () => {
    render(<HistoryTable data={sampleData} />);
    expect(screen.getByText('2025-02-01')).toBeTruthy();
    expect(screen.getByText('2025-01-15')).toBeTruthy();
  });

  it('does not show breakdown rows before click (RDAT-03)', () => {
    render(<HistoryTable data={sampleData} />);
    expect(screen.queryByText('Checking')).toBeNull();
    expect(screen.queryByText('ISA')).toBeNull();
  });

  it('expands per-item breakdown on row click (RDAT-03)', async () => {
    const user = userEvent.setup();
    render(<HistoryTable data={sampleData} />);
    const firstRow = screen.getByText('2025-02-01').closest('tr');
    await user.click(firstRow!);
    expect(screen.getByText('Checking')).toBeTruthy();
  });

  it('collapses row on second click (RDAT-03)', async () => {
    const user = userEvent.setup();
    render(<HistoryTable data={sampleData} />);
    const firstRow = screen.getByText('2025-02-01').closest('tr');
    await user.click(firstRow!);
    await user.click(firstRow!);
    expect(screen.queryByText('Checking')).toBeNull();
  });
});
```

**Step 7 — Create `client/src/__tests__/LiabilitiesPage.test.tsx`** (stubs covering RDAT-04, RDAT-05):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import MockAdapter from 'axios-mock-adapter';

vi.mock('@/components/ui/dialog', () => ({
  Dialog: ({ children, open }: { children: React.ReactNode; open: boolean }) =>
    open ? <div data-testid="dialog">{children}</div> : null,
  DialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/alert-dialog', () => ({
  AlertDialog: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogDescription: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogCancel: ({ children }: { children: React.ReactNode }) => <button>{children}</button>,
  AlertDialogAction: ({ children, onClick }: { children: React.ReactNode; onClick?: () => void }) => <button onClick={onClick}>{children}</button>,
}));

vi.mock('@/components/ui/calendar', () => ({
  Calendar: () => <div data-testid="calendar" />,
}));

vi.mock('@/components/ui/popover', () => ({
  Popover: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/select', () => ({
  Select: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectValue: ({ placeholder }: { placeholder?: string }) => <span>{placeholder}</span>,
  SelectContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectItem: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/table', () => ({
  Table: ({ children }: { children: React.ReactNode }) => <table>{children}</table>,
  TableHeader: ({ children }: { children: React.ReactNode }) => <thead>{children}</thead>,
  TableBody: ({ children }: { children: React.ReactNode }) => <tbody>{children}</tbody>,
  TableHead: ({ children }: { children: React.ReactNode }) => <th>{children}</th>,
  TableRow: ({ children, onClick, className }: { children: React.ReactNode; onClick?: () => void; className?: string }) => <tr onClick={onClick} className={className}>{children}</tr>,
  TableCell: ({ children, className }: { children: React.ReactNode; className?: string }) => <td className={className}>{children}</td>,
}));

import React from 'react';
import { screen, waitFor } from '@testing-library/react';
import { renderWithQuery } from './test-utils';
import { apiClient } from '@/lib/apiClient';
import { LiabilitiesPage } from '@/pages/LiabilitiesPage';

const mockAxios = new MockAdapter(apiClient);

describe('LiabilitiesPage', () => {
  beforeEach(() => { mockAxios.reset(); });
  afterEach(() => { vi.clearAllMocks(); });

  it('renders liability type list from API (RDAT-04)', async () => {
    mockAxios.onGet('/api/liabilities/types').reply(200, [
      { id: 1, name: 'Mortgage', in_use: true },
      { id: 2, name: 'Car Loan', in_use: false },
    ]);
    mockAxios.onGet('/api/liabilities/history').reply(200, []);
    renderWithQuery(<LiabilitiesPage />);
    await waitFor(() => {
      expect(screen.getByText('Mortgage')).toBeTruthy();
      expect(screen.getByText('Car Loan')).toBeTruthy();
    });
  });

  it('renders page title "Liabilities" (RDAT-04)', async () => {
    mockAxios.onGet('/api/liabilities/types').reply(200, []);
    mockAxios.onGet('/api/liabilities/history').reply(200, []);
    renderWithQuery(<LiabilitiesPage />);
    await waitFor(() => {
      expect(screen.getByText('Liabilities')).toBeTruthy();
    });
  });
});
```

**Step 8 — Create `client/src/__tests__/PensionPage.test.tsx`** (stubs covering RDAT-06, RDAT-07):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import MockAdapter from 'axios-mock-adapter';

vi.mock('@/components/ui/dialog', () => ({
  Dialog: ({ children, open }: { children: React.ReactNode; open: boolean }) =>
    open ? <div data-testid="dialog">{children}</div> : null,
  DialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  DialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/alert-dialog', () => ({
  AlertDialog: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogHeader: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogTitle: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogDescription: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogFooter: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  AlertDialogCancel: ({ children }: { children: React.ReactNode }) => <button>{children}</button>,
  AlertDialogAction: ({ children, onClick }: { children: React.ReactNode; onClick?: () => void }) => <button onClick={onClick}>{children}</button>,
}));

vi.mock('@/components/ui/calendar', () => ({
  Calendar: () => <div data-testid="calendar" />,
}));

vi.mock('@/components/ui/popover', () => ({
  Popover: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  PopoverContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/select', () => ({
  Select: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectTrigger: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectValue: ({ placeholder }: { placeholder?: string }) => <span>{placeholder}</span>,
  SelectContent: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  SelectItem: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}));

vi.mock('@/components/ui/table', () => ({
  Table: ({ children }: { children: React.ReactNode }) => <table>{children}</table>,
  TableHeader: ({ children }: { children: React.ReactNode }) => <thead>{children}</thead>,
  TableBody: ({ children }: { children: React.ReactNode }) => <tbody>{children}</tbody>,
  TableHead: ({ children }: { children: React.ReactNode }) => <th>{children}</th>,
  TableRow: ({ children, onClick, className }: { children: React.ReactNode; onClick?: () => void; className?: string }) => <tr onClick={onClick} className={className}>{children}</tr>,
  TableCell: ({ children, className }: { children: React.ReactNode; className?: string }) => <td className={className}>{children}</td>,
}));

import React from 'react';
import { screen, waitFor } from '@testing-library/react';
import { renderWithQuery } from './test-utils';
import { apiClient } from '@/lib/apiClient';
import { PensionPage } from '@/pages/PensionPage';

const mockAxios = new MockAdapter(apiClient);

describe('PensionPage', () => {
  beforeEach(() => { mockAxios.reset(); });
  afterEach(() => { vi.clearAllMocks(); });

  it('renders pension provider list from API (RDAT-06)', async () => {
    mockAxios.onGet('/api/pension/types').reply(200, [
      { id: 1, name: 'NEST', is_pension: true },
      { id: 2, name: 'Company Pension', is_pension: true },
    ]);
    mockAxios.onGet('/api/pension/history').reply(200, []);
    renderWithQuery(<PensionPage />);
    await waitFor(() => {
      expect(screen.getByText('NEST')).toBeTruthy();
      expect(screen.getByText('Company Pension')).toBeTruthy();
    });
  });

  it('renders page title "Pension" (RDAT-06)', async () => {
    mockAxios.onGet('/api/pension/types').reply(200, []);
    mockAxios.onGet('/api/pension/history').reply(200, []);
    renderWithQuery(<PensionPage />);
    await waitFor(() => {
      expect(screen.getByText('Pension')).toBeTruthy();
    });
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/lib/queryClient.ts` exists and contains `new QueryClient(`
    - `client/src/main.tsx` contains `QueryClientProvider` and `queryClient`
    - `client/src/__tests__/test-utils.tsx` exists and contains `export function renderWithQuery`
    - `client/src/__tests__/AccountsPage.test.tsx` exists and contains `describe('AccountsPage'`
    - `client/src/__tests__/BalanceEntryForm.test.tsx` exists and contains `describe('BalanceEntryForm'`
    - `client/src/__tests__/HistoryTable.test.tsx` exists and contains `describe('HistoryTable'`
    - `client/src/__tests__/LiabilitiesPage.test.tsx` exists and contains `describe('LiabilitiesPage'`
    - `client/src/__tests__/PensionPage.test.tsx` exists and contains `describe('PensionPage'`
    - `cd client && npm run build` exits 0 (no TypeScript errors from main.tsx change)
    - `cd client && npx vitest run` exits 0 — all pre-existing tests (PrivateRoute, apiClient, AppSidebar) still pass; new test stubs either pass or are skipped (they will be marked as failing until Plan 02 implements the components, which is acceptable — stubs must at least compile and be collected)
  </acceptance_criteria>
  <done>All 8 files created. main.tsx wraps App with QueryClientProvider. npm run build exits 0. npx vitest run collects all test files without collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → client | Third-party packages installed from registry; version pins prevent supply chain drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | npm install (react-day-picker peer) | accept | react-day-picker@8.10.1 works with React 19 at runtime; peer warning is cosmetic; `--legacy-peer-deps` only overrides semver check, not code execution |
| T-12-01-02 | Tampering | QueryClientProvider placement | mitigate | QueryClientProvider wraps App in main.tsx — outside AuthProvider so auth state cannot be bypassed by manipulating cache |
</threat_model>

<verification>
After both tasks complete:

```bash
# Package versions
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm ls @tanstack/react-query react-hook-form date-fns 2>/dev/null | grep -E "query|hook-form|date-fns"

# shadcn components present
ls /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/dialog.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/form.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/calendar.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/popover.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/table.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/label.tsx \
   /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/ui/select.tsx

# QueryClientProvider wired
grep -n "QueryClientProvider" /Users/kristiakarakatsani/Repos/finance-tracker/client/src/main.tsx

# queryClient singleton
grep -n "new QueryClient" /Users/kristiakarakatsani/Repos/finance-tracker/client/src/lib/queryClient.ts

# Test helper
grep -n "renderWithQuery" /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/test-utils.tsx

# Build and test
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm run build && npx vitest run
```
</verification>

<success_criteria>
1. `@tanstack/react-query@5.100.10` installed — `npm ls @tanstack/react-query` confirms version
2. `date-fns@2.x` installed — `npm ls date-fns` shows v2
3. All 7 shadcn component files exist in `client/src/components/ui/`
4. `client/src/main.tsx` contains `QueryClientProvider client={queryClient}` wrapping App
5. `client/src/lib/queryClient.ts` exports `queryClient` singleton with staleTime:30000
6. `client/src/__tests__/test-utils.tsx` exports `renderWithQuery` with `retry: false`
7. All 5 test stub files exist and are collected by vitest without import errors
8. `cd client && npm run build` exits 0 — no TypeScript errors
9. `cd client && npx vitest run` exits 0 — all pre-existing tests still pass
</success_criteria>

<output>
After completion, create `/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/12-data-pages/12-01-SUMMARY.md`
</output>
