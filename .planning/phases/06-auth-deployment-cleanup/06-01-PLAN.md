---
phase: 06-auth-deployment-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/main.py
  - scripts/migrate_test_user.py
  - .planning/REQUIREMENTS.md
  - app/services/auth_service.py
  - .planning/phases/04-firebase-authentication/04-01-SUMMARY.md
  - .planning/phases/04-firebase-authentication/04-02-SUMMARY.md
  - .planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md
autonomous: true
requirements: [AUTH-04, DEPLOY-05]

must_haves:
  truths:
    - "frontend/main.py authenticated path calls get_or_create_user(...) without assigning the return value — no dead user_id variable"
    - "scripts/migrate_test_user.py does not exist on disk"
    - "REQUIREMENTS.md shows [x] for AUTH-01 through AUTH-06, with AUTH-04 traceability showing Complete"
    - "REQUIREMENTS.md DEPLOY-05 traceability row shows Complete"
    - "04-01-SUMMARY.md and 04-02-SUMMARY.md contain requirements-completed frontmatter fields"
    - "05-01-SUMMARY.md uses requirements-completed (not requirements) as the frontmatter key"
  artifacts:
    - path: "frontend/main.py"
      provides: "Auth gate without dead user_id variable or debug output"
      contains: "get_or_create_user(session, uid, email, name)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Fully checked AUTH-01–AUTH-06 requirements with Complete traceability"
      contains: "[x] **AUTH-04**"
    - path: ".planning/phases/04-firebase-authentication/04-01-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed: [AUTH-01, AUTH-06]"
    - path: ".planning/phases/04-firebase-authentication/04-02-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed: [AUTH-02, AUTH-03, AUTH-04, AUTH-05]"
    - path: ".planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md"
      provides: "corrected requirements-completed frontmatter key"
      contains: "requirements-completed: [DEPLOY-02]"
  key_links:
    - from: "frontend/main.py"
      to: "get_or_create_user"
      via: "side-effect call only"
      pattern: "get_or_create_user\\(session, uid, email, name\\)"
---

<objective>
Close all v1.0 audit gaps identified in .planning/v1.0-MILESTONE-AUDIT.md.

Purpose: The audit found two requirement-level gaps (AUTH-04 dead code, DEPLOY-05 stale script) and four documentation tracking issues (unchecked REQUIREMENTS.md boxes, wrong frontmatter key in 05-01-SUMMARY.md, missing requirements-completed in 04-01 and 04-02 SUMMARY.md files). This plan resolves all six.

Output: Clean auth gate with no dead variable, deleted stale migration script, fully checked and traced REQUIREMENTS.md, and correct SUMMARY.md frontmatter across three phase files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-auth-deployment-cleanup/06-RESEARCH.md

<interfaces>
<!-- Key code pattern confirmed in 06-RESEARCH.md. -->
<!-- From frontend/main.py (authenticated path, lines ~121-131): -->
```python
session = next(get_session())
try:
    user_id = get_or_create_user(session, uid, email, name)  # dead variable — remove assignment only
except ValueError as e:
    st.error(f"Authentication error: {e}")
    st.stop()
finally:
    session.close()

st.session_state["user_id"] = uid  # set from uid, not user_id
```

<!-- After edit (keep call, drop assignment): -->
```python
session = next(get_session())
try:
    get_or_create_user(session, uid, email, name)
except ValueError as e:
    st.error(f"Authentication error: {e}")
    st.stop()
finally:
    session.close()
```

<!-- CRITICAL: Do NOT remove the get_or_create_user() call itself. -->
<!-- It blocks 'test-user' via ValueError — removing the call loses the production guard. -->
<!-- Only remove the `user_id = ` assignment prefix. -->

<!-- Correct SUMMARY.md frontmatter key (from 05-02-SUMMARY.md confirmed pattern): -->
```yaml
requirements-completed: [AUTH-01, AUTH-06]
```
<!-- NOT `requirements:` — the audit found 05-01-SUMMARY.md used the wrong key. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and delete stale script (AUTH-04, DEPLOY-05)</name>
  <files>frontend/main.py, scripts/migrate_test_user.py</files>
  <action>
Two surgical changes:

**Change 1 — frontend/main.py:**
Read the file first to confirm current state. Find the authenticated path block where `user_id = get_or_create_user(session, uid, email, name)` appears. Remove only the `user_id = ` assignment prefix — keep the function call `get_or_create_user(session, uid, email, name)` intact (it blocks test-user via side effect). The except ValueError handler and finally block must stay unchanged. Also confirm no `st.sidebar.write` debug calls exist in the unauthenticated path (they should already be absent per research).

The exact before/after from RESEARCH.md:
- Before: `user_id = get_or_create_user(session, uid, email, name)`
- After: `get_or_create_user(session, uid, email, name)`

**Change 2 — scripts/migrate_test_user.py:**
Delete this file entirely. It references `DELETE FROM users WHERE id = 'test-user'` against a table that no longer exists. Cloud SQL is confirmed empty (Phase 05-04 decision: "no migration needed"). Use bash: `rm scripts/migrate_test_user.py`

After both changes, run `pytest tests/ -x -q` to confirm service tests still pass.
  </action>
  <verify>
    <automated>python -c "import os; assert not os.path.exists('scripts/migrate_test_user.py'), 'stale script still exists'" && grep -n "user_id = get_or_create_user" frontend/main.py && echo "FAIL: dead variable still present" || echo "PASS: dead variable removed" && pytest tests/ -x -q</automated>
  </verify>
  <done>scripts/migrate_test_user.py does not exist. frontend/main.py authenticated path contains `get_or_create_user(session, uid, email, name)` without `user_id =` prefix. pytest tests/ passes green.</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation — REQUIREMENTS.md checkboxes, auth_service docstring, SUMMARY.md frontmatter</name>
  <files>.planning/REQUIREMENTS.md, app/services/auth_service.py, .planning/phases/04-firebase-authentication/04-01-SUMMARY.md, .planning/phases/04-firebase-authentication/04-02-SUMMARY.md, .planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md</files>
  <action>
Five documentation edits, all text changes:

**1. .planning/REQUIREMENTS.md:**
Read the file first. Make these exact changes:
- Line with `[ ] **AUTH-04**` → change to `[x] **AUTH-04**` (AUTH-01 through AUTH-03, AUTH-05, AUTH-06 should already be `[x]` per current state — confirm and check any that remain unchecked)
- Traceability table row `AUTH-04 | Phase 6 | Pending` → `AUTH-04 | Phase 6 | Complete`
- Traceability table row `DEPLOY-05 | Phase 6 | Pending` → `DEPLOY-05 | Phase 6 | Complete`
- Do NOT touch DEPLOY-04 row — it remains Pending (human verification gate still open from Phase 05-03)

**2. app/services/auth_service.py:**
Read the current docstring for `get_or_create_user`. Confirm it already states the function validates the user (not creates). If the docstring still implies DB writes or "create" behavior, update it to clearly state: "Validates the user ID (blocks test-user) and returns the uid. Does not write to the database — the users table no longer exists." Per research, the current docstring is already largely accurate; a minor wording confirmation/tweak is all that's needed.

**3. .planning/phases/04-firebase-authentication/04-01-SUMMARY.md:**
Read the frontmatter block (between `---` delimiters). Add field:
```yaml
requirements-completed: [AUTH-01, AUTH-06]
```
Insert this after the existing frontmatter fields. Maintain valid YAML.

**4. .planning/phases/04-firebase-authentication/04-02-SUMMARY.md:**
Read the frontmatter block. Add field:
```yaml
requirements-completed: [AUTH-02, AUTH-03, AUTH-04, AUTH-05]
```

**5. .planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md:**
Read the frontmatter block. Find `requirements: [DEPLOY-02]` (non-standard key). Change to:
```yaml
requirements-completed: [DEPLOY-02]
```

After all edits, run `pytest tests/ -x -q` again to confirm nothing broken.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*AUTH-04\*\*" .planning/REQUIREMENTS.md && grep -c "requirements-completed" .planning/phases/04-firebase-authentication/04-01-SUMMARY.md && grep -c "requirements-completed" .planning/phases/04-firebase-authentication/04-02-SUMMARY.md && grep "requirements-completed" .planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md && pytest tests/ -x -q</automated>
  </verify>
  <done>REQUIREMENTS.md has [x] for AUTH-04 and Complete in traceability for AUTH-04 and DEPLOY-05. All three SUMMARY.md files have requirements-completed field with correct key name. pytest tests/ green.</done>
</task>

</tasks>

<verification>
Run the full test suite after all tasks complete:

```bash
pytest tests/
```

Manual code review checks:
1. Read `frontend/main.py` — confirm no `user_id =` before `get_or_create_user(...)`, no `st.sidebar.write` debug calls in unauthenticated path
2. Read `.planning/REQUIREMENTS.md` — confirm AUTH-01 through AUTH-06 all `[x]`, traceability for AUTH-04 and DEPLOY-05 show "Complete"
3. Read `app/services/auth_service.py` — confirm `get_or_create_user` docstring reflects validation behavior
4. Read `.planning/phases/04-firebase-authentication/04-01-SUMMARY.md` — confirm `requirements-completed: [AUTH-01, AUTH-06]`
5. Read `.planning/phases/04-firebase-authentication/04-02-SUMMARY.md` — confirm `requirements-completed: [AUTH-02, AUTH-03, AUTH-04, AUTH-05]`
6. Read `.planning/phases/05-cloud-run-deployment/05-01-SUMMARY.md` — confirm `requirements-completed: [DEPLOY-02]` (not `requirements:`)
7. Confirm `scripts/migrate_test_user.py` does not exist: `ls scripts/`
</verification>

<success_criteria>
Phase 6 is complete when:
1. `frontend/main.py` authenticated path calls `get_or_create_user(session, uid, email, name)` with no return value assignment — dead variable gone
2. `scripts/migrate_test_user.py` does not exist
3. REQUIREMENTS.md: AUTH-01 through AUTH-06 all `[x]`; AUTH-04 traceability = Complete; DEPLOY-05 traceability = Complete
4. `get_or_create_user` docstring clearly describes validation-only behavior (no DB writes)
5. 04-01-SUMMARY.md has `requirements-completed: [AUTH-01, AUTH-06]`
6. 04-02-SUMMARY.md has `requirements-completed: [AUTH-02, AUTH-03, AUTH-04, AUTH-05]`
7. 05-01-SUMMARY.md has `requirements-completed: [DEPLOY-02]` (hyphenated key, not `requirements`)
8. `pytest tests/` green
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-deployment-cleanup/06-01-SUMMARY.md`
</output>
