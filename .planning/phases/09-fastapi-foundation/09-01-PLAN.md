---
phase: 09-fastapi-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - api/__init__.py
  - api/main.py
  - api/dependencies.py
  - api/routers/__init__.py
  - api/routers/health.py
  - api/schemas/__init__.py
  - api/schemas/health.py
  - app/database.py
  - tests/test_api_health.py
  - tests/test_api_auth.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "GET /api/health returns HTTP 200 with {\"status\": \"ok\"}"
    - "Firebase Admin SDK is initialised inside the lifespan context manager, never at module level"
    - "A CORS preflight from http://localhost:5173 returns Access-Control-Allow-Origin: http://localhost:5173"
    - "A CORS preflight from an unlisted origin returns no Access-Control-Allow-Origin header"
    - "A request without an Authorization header to a protected route returns HTTP 401"
    - "A request with a valid token but wrong UID returns HTTP 403"
    - "A Pydantic schema with a float balance field serialises to a JSON number, not a string"
    - "pool_pre_ping=True is set on the database engine in app/database.py"
  artifacts:
    - path: "api/__init__.py"
      provides: "api package marker"
    - path: "api/main.py"
      provides: "FastAPI app with lifespan, CORS middleware, router includes"
      contains: "lifespan"
    - path: "api/dependencies.py"
      provides: "get_current_user auth dependency"
      exports: ["get_current_user"]
    - path: "api/routers/health.py"
      provides: "GET /api/health endpoint"
      contains: "/health"
    - path: "api/schemas/health.py"
      provides: "HealthResponse Pydantic schema"
      exports: ["HealthResponse"]
    - path: "tests/test_api_health.py"
      provides: "Tests for health endpoint, CORS, lifespan, float serialisation"
    - path: "tests/test_api_auth.py"
      provides: "Tests for auth dependency (401/403 scenarios, dev bypass)"
  key_links:
    - from: "api/main.py"
      to: "api/dependencies.py"
      via: "get_current_user imported and used in routes that require auth"
      pattern: "from api.dependencies import get_current_user"
    - from: "api/main.py"
      to: "firebase_admin.initialize_app"
      via: "lifespan context manager — not module level"
      pattern: "asynccontextmanager"
    - from: "api/dependencies.py"
      to: "settings.dev_user_id"
      via: "dev bypass — returns dev_user_id before touching Firebase"
      pattern: "settings.dev_user_id"
---

<objective>
Install FastAPI, scaffold the api/ package, implement the server infrastructure (lifespan Firebase init, CORS middleware, auth dependency, health endpoint, pool_pre_ping), and write the full test suite. Tests are written first (RED), then implementation makes them GREEN.

Purpose: Establish the FastAPI server foundation — correct configuration across CORS, auth, Firebase Admin SDK lifecycle, and Decimal serialisation — before any feature routes are written.
Output: Working FastAPI server with passing test suite covering all four phase requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fastapi-foundation/09-CONTEXT.md
@.planning/phases/09-fastapi-foundation/09-RESEARCH.md

<interfaces>
<!-- Existing code contracts the executor uses directly. No codebase exploration needed. -->

From app/config.py:
```python
class Settings(BaseSettings):
    database_url: str
    firebase_credentials_path: str = ""
    firebase_web_api_key: str = ""
    firebase_auth_domain: str = ""
    firebase_project_id: str = ""
    debug: bool = False
    dev_user_id: str = ""
    allowed_firebase_uid: str = ""
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

settings = Settings()
```

From app/database.py (current — pool_pre_ping NOT yet set):
```python
engine = create_engine(settings.database_url, echo=settings.debug)

def get_session() -> Generator[Session, None, None]:
    with Session(engine) as session:
        yield session
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests (RED) and install fastapi[standard]</name>
  <files>tests/test_api_health.py, tests/test_api_auth.py, pyproject.toml, uv.lock</files>
  <behavior>
    - test_health_returns_200: GET /api/health returns 200, body is {"status": "ok"}
    - test_firebase_not_init_at_import: importing api.main does NOT trigger firebase_admin.initialize_app — verified by checking firebase_admin.apps is empty immediately after import (before TestClient context manager runs)
    - test_cors_preflight_allowed_origin: OPTIONS /api/health with Origin: http://localhost:5173 and Access-Control-Request-Method: GET returns 200 and response header Access-Control-Allow-Origin == "http://localhost:5173"
    - test_cors_preflight_rejected_origin: OPTIONS /api/health with Origin: http://evil.example.com returns a response with NO Access-Control-Allow-Origin header
    - test_missing_token_returns_401: GET /api/protected (any route requiring auth) with no Authorization header returns 401
    - test_invalid_token_returns_401: GET /api/protected with Authorization: Bearer invalidtoken returns 401; mocker.patch("firebase_admin.auth.verify_id_token", side_effect=Exception("bad")) is used to simulate invalid token
    - test_wrong_uid_returns_403: GET /api/protected with a valid token but UID != settings.allowed_firebase_uid returns 403; mocker.patch returns decoded = {"uid": "wrong-uid"}; settings.allowed_firebase_uid must be non-empty for this test (patch settings)
    - test_dev_bypass_skips_firebase: when settings.dev_user_id is non-empty, GET /api/protected succeeds (200) without any Authorization header and firebase_admin.auth.verify_id_token is never called
    - test_decimal_serialises_as_float: a Pydantic BaseModel with balance: float field, when constructed with balance=float(Decimal("10753.42")), serialises to {"balance": 10753.42} (a JSON number, not a string)
  </behavior>
  <action>
    1. Run: uv add "fastapi[standard]"
    2. Create tests/test_api_health.py covering: health_returns_200, firebase_not_init_at_import, cors_preflight_allowed_origin, cors_preflight_rejected_origin, decimal_serialises_as_float.

    For the firebase_not_init_at_import test, import firebase_admin first, confirm firebase_admin.apps is empty, then import api.main (without creating a TestClient — just the import). The TestClient context manager runs the lifespan which initialises Firebase; we are testing the import, not the lifespan.

    For CORS preflight tests, use TestClient directly (no lifespan needed for preflight — CORSMiddleware intercepts before hitting routes). Use `with TestClient(app) as client:` to trigger lifespan during the test.

    For decimal_serialises_as_float: import HealthResponse from api.schemas.health; this test proves Pydantic float field works — it does not need the running server.

    3. Create tests/test_api_auth.py covering: missing_token_returns_401, invalid_token_returns_401, wrong_uid_returns_403, dev_bypass_skips_firebase.

    For auth tests, the api/ package does not yet exist when these tests are written — tests will fail at import. That is the intended RED state. Use a placeholder protected route for testing auth: either GET /api/health is sufficient (it is public — so for auth tests, add a minimal protected route in the test file itself via app.dependency_overrides or create a temporary test route that requires get_current_user).

    Better approach: in test_api_auth.py, after importing app from api.main, add a test route inline:
    ```python
    from fastapi import Depends
    from api.dependencies import get_current_user
    from api.main import app

    @app.get("/api/test-protected")
    def _protected(uid: str = Depends(get_current_user)):
        return {"uid": uid}
    ```
    This registers a route for testing without polluting the production router.

    For wrong_uid_returns_403: monkeypatch settings.allowed_firebase_uid to "expected-uid" and patch verify_id_token to return {"uid": "wrong-uid"}.
    For dev_bypass: monkeypatch settings.dev_user_id to "dev-user".

    4. Run: pytest tests/test_api_health.py tests/test_api_auth.py -x
       All tests must FAIL at import (ImportError: No module named 'api') — confirm RED state.
  </action>
  <verify>
    <automated>pytest tests/test_api_health.py tests/test_api_auth.py -x 2>&1 | grep -E "ERROR|ImportError|ModuleNotFoundError" | head -5</automated>
  </verify>
  <done>Test files exist, fastapi[standard] is installed in pyproject.toml, and running pytest on the test files fails with ImportError (no api module yet) — confirming RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement api/ package (GREEN)</name>
  <files>api/__init__.py, api/main.py, api/dependencies.py, api/routers/__init__.py, api/routers/health.py, api/schemas/__init__.py, api/schemas/health.py, app/database.py</files>
  <behavior>
    - api/__init__.py: empty package marker
    - api/schemas/health.py: HealthResponse(BaseModel) with status: str field
    - api/routers/health.py: APIRouter with prefix="/api"; GET /health returns HealthResponse(status="ok"); no auth dependency
    - api/dependencies.py: get_current_user dependency using HTTPBearer(auto_error=False); dev bypass when settings.dev_user_id is truthy; raises 401 for missing/invalid token; raises 403 if decoded UID != settings.allowed_firebase_uid (when allowed_firebase_uid is non-empty)
    - api/main.py: FastAPI(lifespan=lifespan); lifespan initialises Firebase Admin SDK inside the async context manager (not at module level); lifespan skips Firebase init when settings.dev_user_id is truthy (test/dev bypass); CORSMiddleware with allow_origins=["http://localhost:5173", "https://finance-tracker-rntookejza.web.app", "https://finance-tracker-rntookejza.firebaseapp.com"], allow_credentials=True, allow_methods=["GET","POST","PUT","PATCH","DELETE","OPTIONS"], allow_headers=["Authorization","Content-Type","Accept"]; includes health router
    - app/database.py: add pool_pre_ping=True to create_engine() call
  </behavior>
  <action>
    Create files in this order (dependencies first):

    1. api/__init__.py — empty file

    2. api/schemas/__init__.py — empty file

    3. api/schemas/health.py:
    ```python
    from pydantic import BaseModel

    class HealthResponse(BaseModel):
        status: str
    ```

    4. api/routers/__init__.py — empty file

    5. api/routers/health.py:
    ```python
    from fastapi import APIRouter
    from api.schemas.health import HealthResponse

    router = APIRouter(prefix="/api")

    @router.get("/health", response_model=HealthResponse)
    def health_check() -> HealthResponse:
        """Public health endpoint — no auth required."""
        return HealthResponse(status="ok")
    ```

    6. api/dependencies.py — implement get_current_user per Pattern 3 in RESEARCH.md. Key points:
       - bearer_scheme = HTTPBearer(auto_error=False)
       - If settings.dev_user_id is truthy: return settings.dev_user_id immediately (skip Firebase entirely)
       - If no token: raise HTTPException(401, "Missing credentials", headers={"WWW-Authenticate": "Bearer"})
       - Call firebase_admin.auth.verify_id_token(token.credentials); on any exception raise 401
       - If settings.allowed_firebase_uid and uid != settings.allowed_firebase_uid: raise 403
       - Return uid as str

    7. api/main.py — implement per Pattern 1 and 2 in RESEARCH.md. Critical: lifespan must guard Firebase init with `if not settings.dev_user_id:` so tests with DEV_USER_ID set never call initialize_app. On shutdown call `firebase_admin.delete_app(firebase_admin.get_app())` only if Firebase was initialised (i.e., only when not dev bypass).

    8. app/database.py — change:
       `engine = create_engine(settings.database_url, echo=settings.debug)`
       to:
       `engine = create_engine(settings.database_url, echo=settings.debug, pool_pre_ping=True)`

    Use snake_case, Sphinx docstrings on public functions, `str | None` syntax (Python 3.12 style). No loguru needed in this phase.

    After all files created, run the test suite.
  </action>
  <verify>
    <automated>pytest tests/test_api_health.py tests/test_api_auth.py -x -v</automated>
  </verify>
  <done>All tests in test_api_health.py and test_api_auth.py pass. pytest reports 0 failures. app/database.py has pool_pre_ping=True. api/ package is fully scaffolded.</done>
</task>

</tasks>

<verification>
Full test suite must remain green after this plan:

```bash
pytest tests/ -x
```

Spot-check: import api.main without TestClient; confirm firebase_admin.apps is empty (no module-level init).
</verification>

<success_criteria>
- All 9 tests in test_api_health.py and test_api_auth.py pass
- GET /api/health returns {"status": "ok"} via TestClient
- CORS preflight from http://localhost:5173 succeeds with correct header
- CORS preflight from unlisted origin has no Access-Control-Allow-Origin header
- Missing token returns 401, invalid token returns 401, wrong UID returns 403
- Dev bypass returns 200 without a token (settings.dev_user_id set)
- Float field in HealthResponse serialises as JSON number not string
- pool_pre_ping=True is in app/database.py
- Full pytest suite passes (existing service tests still green)
</success_criteria>

<output>
After completion, create `.planning/phases/09-fastapi-foundation/09-01-SUMMARY.md`
</output>
