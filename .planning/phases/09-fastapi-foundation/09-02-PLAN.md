---
phase: 09-fastapi-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docker-compose.yml
autonomous: false
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "docker-compose up starts both the Streamlit app (port 8501) and the FastAPI server (port 8000) without errors"
    - "curl http://localhost:8000/api/health returns HTTP 200 with {\"status\": \"ok\"}"
    - "curl with -H \"Authorization: Bearer faketoken\" against /api/health still returns 200 (health is public)"
    - "CORS preflight from http://localhost:5173 returns Access-Control-Allow-Origin: http://localhost:5173 in the response"
  artifacts:
    - path: "docker-compose.yml"
      provides: "api service definition on port 8000"
      contains: "uvicorn api.main:app --host 0.0.0.0 --port 8000"
  key_links:
    - from: "docker-compose.yml api service"
      to: "api/main.py"
      via: "uvicorn api.main:app command"
      pattern: "uvicorn api.main:app"
    - from: "docker-compose.yml api service"
      to: "db service"
      via: "depends_on: db and DATABASE_URL env var"
      pattern: "depends_on.*db"
---

<objective>
Add the FastAPI api service to docker-compose.yml and verify the running server end-to-end via curl — confirming health endpoint, auth gating, and CORS headers all work correctly against the live server.

Purpose: Prove the FastAPI server boots correctly in the Docker environment alongside Streamlit before Phase 10 adds feature routes.
Output: docker-compose.yml updated; human confirms the server is reachable and correct.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-fastapi-foundation/09-CONTEXT.md
@.planning/phases/09-fastapi-foundation/09-01-SUMMARY.md

<interfaces>
<!-- Current docker-compose.yml structure (before this plan) -->
```yaml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: finance
      POSTGRES_PASSWORD: finance
      POSTGRES_DB: finance_tracker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/.venv
    environment:
      DATABASE_URL: postgresql://finance:finance@db:5432/finance_tracker
      FIREBASE_CREDENTIALS_PATH: ""
      DEBUG: "true"
      DEV_USER_ID: "local-dev-user"
      STREAMLIT_SERVER_FILE_WATCHER_TYPE: "poll"
    depends_on:
      - db

volumes:
  pgdata:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add api service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Add an `api` service to docker-compose.yml. The service must:
    - Use the same build context (`.`) as the app service — Dockerfile is NOT changed in this phase
    - Map port 8000:8000
    - Mount volumes `.:/app` and `/app/.venv` (identical to app service — hot reload works)
    - Set environment variables:
        DATABASE_URL: postgresql://finance:finance@db:5432/finance_tracker
        FIREBASE_CREDENTIALS_PATH: ""
        DEBUG: "true"
        DEV_USER_ID: "local-dev-user"
        ALLOWED_FIREBASE_UID: ""
    - Add `depends_on: - db`
    - Set command: `uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload`
      CRITICAL: `--host 0.0.0.0` is required — uvicorn defaults to 127.0.0.1 inside Docker, making the port unreachable from the host.

    The existing `app` (Streamlit) and `db` services are NOT modified. Preserve all existing service definitions exactly.
  </action>
  <verify>
    <automated>python -c "import yaml; cfg = yaml.safe_load(open('/Users/kristiakarakatsani/Repos/finance-tracker/docker-compose.yml')); assert 'api' in cfg['services'], 'api service missing'; cmd = cfg['services']['api']['command']; assert '--host 0.0.0.0' in cmd, 'missing --host 0.0.0.0'; assert '8000' in cmd, 'wrong port'; print('docker-compose.yml OK')"</automated>
  </verify>
  <done>docker-compose.yml contains api service with correct command including --host 0.0.0.0 --port 8000 --reload and depends_on db.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    FastAPI server running locally via docker-compose alongside Streamlit. The server has:
    - Public GET /api/health endpoint
    - Firebase auth dependency with dev bypass (DEV_USER_ID=local-dev-user)
    - CORS configured for http://localhost:5173
    - pool_pre_ping=True on the database engine
  </what-built>
  <how-to-verify>
    1. Start services (rebuild to pick up new api service):
       docker-compose up --build

    2. Wait for both "Application startup complete" (FastAPI, port 8000) and Streamlit to be ready.

    3. Health check (should return 200):
       curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/health
       Expected: 200

    4. Health response body (should be JSON):
       curl -s http://localhost:8000/api/health
       Expected: {"status":"ok"}

    5. CORS preflight from allowed origin (should return correct header):
       curl -s -I -X OPTIONS http://localhost:8000/api/health \
         -H "Origin: http://localhost:5173" \
         -H "Access-Control-Request-Method: GET"
       Expected: response contains header: access-control-allow-origin: http://localhost:5173

    6. CORS preflight from rejected origin (should NOT return ACAO header):
       curl -s -I -X OPTIONS http://localhost:8000/api/health \
         -H "Origin: http://evil.example.com" \
         -H "Access-Control-Request-Method: GET"
       Expected: NO access-control-allow-origin header in response

    7. Confirm Streamlit still works at http://localhost:8501 (no regression).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
All automated tests still green after docker-compose changes:

```bash
pytest tests/ -x
```

Human-verified: server reachable on port 8000, health returns 200, CORS headers correct, Streamlit unaffected.
</verification>

<success_criteria>
- docker-compose.yml has api service with --host 0.0.0.0 --port 8000 --reload
- curl http://localhost:8000/api/health returns 200 {"status":"ok"}
- CORS preflight from http://localhost:5173 returns Access-Control-Allow-Origin: http://localhost:5173
- CORS preflight from unlisted origin has no Access-Control-Allow-Origin header
- Streamlit still loads at http://localhost:8501 (no regression)
- Full pytest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-fastapi-foundation/09-02-SUMMARY.md`
</output>
