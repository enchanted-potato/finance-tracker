---
phase: 11-react-scaffold-and-auth
plan: 04
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - client/src/components/AppSidebar.tsx
  - client/src/components/AppLayout.tsx
  - client/src/pages/DashboardPage.tsx
  - client/src/pages/AccountsPage.tsx
  - client/src/pages/LiabilitiesPage.tsx
  - client/src/pages/PensionPage.tsx
  - client/src/pages/HistoryPage.tsx
  - client/src/pages/ConfigurePage.tsx
  - client/src/App.tsx
  - client/src/__tests__/AppSidebar.test.tsx
autonomous: true
requirements:
  - REACT-04

must_haves:
  truths:
    - "AppSidebar renders exactly 6 nav links: Dashboard, Accounts, Liabilities, Pension, History, Configure"
    - "Sidebar nav items appear in the order specified in D-05"
    - "Active nav item renders with text-[#58a6ff] and border-l-2 border-[#58a6ff] via NavLink isActive"
    - "All 6 page stubs resolve — navigating to each route renders the stub heading"
    - "AppLayout wraps sidebar + Outlet so all protected pages share the same layout"
    - "AppSidebar unit tests pass (npx vitest run)"
  artifacts:
    - path: "client/src/components/AppSidebar.tsx"
      provides: "shadcn Sidebar with 6 NavLink items and active class"
      exports: ["AppSidebar"]
    - path: "client/src/components/AppLayout.tsx"
      provides: "Layout shell with fixed sidebar and main content area"
      exports: ["AppLayout"]
    - path: "client/src/pages/DashboardPage.tsx"
      provides: "Dashboard stub (h1 only)"
    - path: "client/src/pages/AccountsPage.tsx"
      provides: "Accounts stub (h1 only)"
    - path: "client/src/pages/LiabilitiesPage.tsx"
      provides: "Liabilities stub (h1 only)"
    - path: "client/src/pages/PensionPage.tsx"
      provides: "Pension stub (h1 only)"
    - path: "client/src/pages/HistoryPage.tsx"
      provides: "History stub (h1 only)"
    - path: "client/src/pages/ConfigurePage.tsx"
      provides: "Configure stub (h1 only)"
    - path: "client/src/__tests__/AppSidebar.test.tsx"
      provides: "Unit tests for REACT-04 (all 6 nav links rendered)"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/components/AppLayout.tsx"
      via: "AppLayout wraps all protected routes as a nested Route"
      pattern: "AppLayout"
    - from: "client/src/components/AppSidebar.tsx"
      to: "react-router-dom NavLink"
      via: "NavLink isActive callback sets active classes"
      pattern: "isActive"
---

<objective>
Implement the sidebar navigation shell and all 6 page stubs. AppSidebar uses shadcn Sidebar primitives with NavLink active class detection. AppLayout provides the shared layout wrapper (sidebar + main content). All routes in App.tsx are updated from placeholder divs to real page components.

Purpose: Satisfies REACT-04 — sidebar with all pages and active highlighting. After this plan the full Phase 11 scope is complete: a navigable SPA with auth gate, sidebar, and 6 stub pages.
Output: AppSidebar, AppLayout, 6 page stubs, updated App.tsx, passing AppSidebar unit tests.
</objective>

<execution_context>
@/Users/kristiakarakatsani/Repos/finance-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-CONTEXT.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-RESEARCH.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-UI-SPEC.md
@/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-VALIDATION.md

<interfaces>
<!-- From Plan 02 outputs — use directly, no codebase exploration needed -->

From client/src/components/PrivateRoute.tsx:
  export function PrivateRoute({ children }: { children: JSX.Element })

From client/src/App.tsx (Plan 02 state — this plan updates it):
  - BrowserRouter + AuthProvider wraps all routes
  - /login → LoginPage (unprotected)
  - /* → PrivateRoute wrapping nested Routes
  - Placeholder divs for each route — THIS PLAN replaces them with real imports

shadcn Sidebar component hierarchy (from RESEARCH.md Pattern 6 — already installed via Plan 01):
  SidebarProvider, Sidebar, SidebarContent, SidebarGroup, SidebarGroupContent,
  SidebarMenu, SidebarMenuItem, SidebarMenuButton
  All imported from '@/components/ui/sidebar'

NavLink active class (react-router-dom v6):
  className={({ isActive }) => isActive ? 'text-[#58a6ff] font-semibold border-l-2 border-[#58a6ff]' : ''}
  end prop required on '/' to avoid matching all routes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppSidebar with NavLink active class, AppLayout, and unit tests</name>
  <files>
    client/src/components/AppSidebar.tsx
    client/src/components/AppLayout.tsx
    client/src/__tests__/AppSidebar.test.tsx
  </files>
  <read_first>
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-RESEARCH.md (Pattern 6: shadcn Sidebar with NavLink — use exact component hierarchy; note collapsible="none" for fixed sidebar)
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-UI-SPEC.md (Layout: sidebar 240px fixed width, bg-card; nav item padding: py-2 px-4 = sm/md; active: text-[#58a6ff] font-semibold border-l-2 border-[#58a6ff])
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-CONTEXT.md (D-04: 6 stubs; D-05: sidebar order Dashboard→Accounts→Liabilities→Pension→History→Configure)
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-VALIDATION.md (AppSidebar.test.tsx — all 6 links must be rendered)
  </read_first>
  <behavior>
    - Test: AppSidebar renders a link with text "Dashboard"
    - Test: AppSidebar renders a link with text "Accounts"
    - Test: AppSidebar renders a link with text "Liabilities"
    - Test: AppSidebar renders a link with text "Pension"
    - Test: AppSidebar renders a link with text "History"
    - Test: AppSidebar renders a link with text "Configure"
    - Test: AppSidebar renders exactly 6 navigation items
  </behavior>
  <action>
**Create `client/src/components/AppSidebar.tsx`** (exact content — D-05 order, active class per UI spec):

```typescript
import { NavLink } from 'react-router-dom';
import {
  Sidebar,
  SidebarContent,
  SidebarGroup,
  SidebarGroupContent,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
} from '@/components/ui/sidebar';

// D-05: Sidebar page order matches existing Streamlit app
const navItems = [
  { to: '/',            label: 'Dashboard' },
  { to: '/accounts',   label: 'Accounts' },
  { to: '/liabilities', label: 'Liabilities' },
  { to: '/pension',    label: 'Pension' },
  { to: '/history',    label: 'History' },
  { to: '/configure',  label: 'Configure' },
];

export function AppSidebar() {
  return (
    <Sidebar collapsible="none" className="w-[240px] bg-card border-r border-border">
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupContent>
            <SidebarMenu>
              {navItems.map(({ to, label }) => (
                <SidebarMenuItem key={to}>
                  <SidebarMenuButton asChild>
                    <NavLink
                      to={to}
                      end={to === '/'}
                      className={({ isActive }) =>
                        isActive
                          ? 'text-[#58a6ff] font-semibold border-l-2 border-[#58a6ff] py-2 px-4 block transition-colors duration-150'
                          : 'py-2 px-4 block transition-colors duration-150 hover:bg-white/5'
                      }
                    >
                      {label}
                    </NavLink>
                  </SidebarMenuButton>
                </SidebarMenuItem>
              ))}
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
    </Sidebar>
  );
}
```

**Create `client/src/components/AppLayout.tsx`** (shared layout wrapper for all protected pages):

```typescript
import { Outlet } from 'react-router-dom';
import { SidebarProvider } from '@/components/ui/sidebar';
import { AppSidebar } from '@/components/AppSidebar';

export function AppLayout() {
  return (
    <SidebarProvider>
      <div className="flex min-h-screen w-full bg-background">
        <AppSidebar />
        <main className="flex-1 p-6 bg-background overflow-auto">
          <Outlet />
        </main>
      </div>
    </SidebarProvider>
  );
}
```

**Create `client/src/__tests__/AppSidebar.test.tsx`**:

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { AppSidebar } from '@/components/AppSidebar';

describe('AppSidebar', () => {
  function renderSidebar() {
    return render(
      <MemoryRouter>
        <AppSidebar />
      </MemoryRouter>
    );
  }

  it('renders a Dashboard nav link', () => {
    renderSidebar();
    expect(screen.getByText('Dashboard')).toBeTruthy();
  });

  it('renders an Accounts nav link', () => {
    renderSidebar();
    expect(screen.getByText('Accounts')).toBeTruthy();
  });

  it('renders a Liabilities nav link', () => {
    renderSidebar();
    expect(screen.getByText('Liabilities')).toBeTruthy();
  });

  it('renders a Pension nav link', () => {
    renderSidebar();
    expect(screen.getByText('Pension')).toBeTruthy();
  });

  it('renders a History nav link', () => {
    renderSidebar();
    expect(screen.getByText('History')).toBeTruthy();
  });

  it('renders a Configure nav link', () => {
    renderSidebar();
    expect(screen.getByText('Configure')).toBeTruthy();
  });

  it('renders exactly 6 navigation items', () => {
    renderSidebar();
    const labels = ['Dashboard', 'Accounts', 'Liabilities', 'Pension', 'History', 'Configure'];
    labels.forEach(label => {
      expect(screen.getByText(label)).toBeTruthy();
    });
    expect(labels).toHaveLength(6);
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All 7 AppSidebar tests pass. All PrivateRoute and apiClient tests from prior plans still pass. `npx vitest run` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: 6 page stubs and App.tsx route wiring update</name>
  <files>
    client/src/pages/DashboardPage.tsx
    client/src/pages/AccountsPage.tsx
    client/src/pages/LiabilitiesPage.tsx
    client/src/pages/PensionPage.tsx
    client/src/pages/HistoryPage.tsx
    client/src/pages/ConfigurePage.tsx
    client/src/App.tsx
  </files>
  <read_first>
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-UI-SPEC.md (Copywriting contract: each stub renders its page name as an h1 heading — exact copy: "Dashboard", "Accounts", "Liabilities", "Pension", "History", "Configure")
    - /Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-CONTEXT.md (D-04: stubs only — no data fetching)
    - client/src/App.tsx (current state from Plan 02 — read before rewriting)
  </read_first>
  <action>
**Create all 6 page stubs** — each is a minimal component with only a heading. No data fetching, no imports beyond React basics.

`client/src/pages/DashboardPage.tsx`:
```typescript
export function DashboardPage() {
  return <h1 className="text-2xl font-semibold">Dashboard</h1>;
}
```

`client/src/pages/AccountsPage.tsx`:
```typescript
export function AccountsPage() {
  return <h1 className="text-2xl font-semibold">Accounts</h1>;
}
```

`client/src/pages/LiabilitiesPage.tsx`:
```typescript
export function LiabilitiesPage() {
  return <h1 className="text-2xl font-semibold">Liabilities</h1>;
}
```

`client/src/pages/PensionPage.tsx`:
```typescript
export function PensionPage() {
  return <h1 className="text-2xl font-semibold">Pension</h1>;
}
```

`client/src/pages/HistoryPage.tsx`:
```typescript
export function HistoryPage() {
  return <h1 className="text-2xl font-semibold">History</h1>;
}
```

`client/src/pages/ConfigurePage.tsx`:
```typescript
export function ConfigurePage() {
  return <h1 className="text-2xl font-semibold">Configure</h1>;
}
```

**Replace `client/src/App.tsx`** with the final route structure using AppLayout and real page components. Replace the Plan 02 placeholder divs:

```typescript
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { AuthProvider } from '@/contexts/AuthContext';
import { PrivateRoute } from '@/components/PrivateRoute';
import { AppLayout } from '@/components/AppLayout';
import { LoginPage } from '@/pages/LoginPage';
import { DashboardPage } from '@/pages/DashboardPage';
import { AccountsPage } from '@/pages/AccountsPage';
import { LiabilitiesPage } from '@/pages/LiabilitiesPage';
import { PensionPage } from '@/pages/PensionPage';
import { HistoryPage } from '@/pages/HistoryPage';
import { ConfigurePage } from '@/pages/ConfigurePage';

export default function App() {
  return (
    <BrowserRouter>
      <AuthProvider>
        <Routes>
          <Route path="/login" element={<LoginPage />} />
          <Route
            path="/*"
            element={
              <PrivateRoute>
                <AppLayout />
              </PrivateRoute>
            }
          >
            <Route index element={<DashboardPage />} />
            <Route path="accounts" element={<AccountsPage />} />
            <Route path="liabilities" element={<LiabilitiesPage />} />
            <Route path="pension" element={<PensionPage />} />
            <Route path="history" element={<HistoryPage />} />
            <Route path="configure" element={<ConfigurePage />} />
          </Route>
        </Routes>
      </AuthProvider>
    </BrowserRouter>
  );
}
```

Note: AppLayout renders `<Outlet />` so nested routes display inside the layout. The PrivateRoute wraps AppLayout — when loading=true it renders null (blank screen), so no sidebar flash occurs.
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm run build && npx vitest run</automated>
  </verify>
  <done>
    - `npm run build` exits 0 — no TypeScript errors, all 6 page stub imports resolve
    - `npx vitest run` exits 0 — all tests pass (PrivateRoute, apiClient, AppSidebar)
    - grep confirms each page stub file contains its expected h1 text
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# All tests pass
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npx vitest run --reporter=verbose

# Build clean
cd /Users/kristiakarakatsani/Repos/finance-tracker/client && npm run build

# Structural checks
ls /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppSidebar.tsx
ls /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppLayout.tsx
ls /Users/kristiakarakatsani/Repos/finance-tracker/client/src/__tests__/AppSidebar.test.tsx

# Page stub checks
grep 'Dashboard' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/DashboardPage.tsx
grep 'Accounts' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/AccountsPage.tsx
grep 'Liabilities' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/LiabilitiesPage.tsx
grep 'Pension' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/PensionPage.tsx
grep 'History' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/HistoryPage.tsx
grep 'Configure' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/pages/ConfigurePage.tsx

# Active class check
grep 'text-\[#58a6ff\]' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppSidebar.tsx
grep 'border-l-2' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppSidebar.tsx
grep 'isActive' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppSidebar.tsx

# Nav order check (D-05)
grep -n 'Dashboard\|Accounts\|Liabilities\|Pension\|History\|Configure' /Users/kristiakarakatsani/Repos/finance-tracker/client/src/components/AppSidebar.tsx
```
</verification>

<success_criteria>
1. `npx vitest run` exits 0 — all tests pass including 7 AppSidebar tests
2. `npm run build` exits 0 — all 6 page stubs imported from App.tsx without TypeScript errors
3. AppSidebar.tsx contains `text-[#58a6ff]` and `border-l-2 border-[#58a6ff]` in the isActive class
4. AppSidebar navItems array contains exactly the 6 routes in D-05 order: /, /accounts, /liabilities, /pension, /history, /configure
5. AppLayout.tsx renders `<Outlet />` so nested routes display inside the layout
6. App.tsx no longer contains inline placeholder divs — uses real page component imports
</success_criteria>

<output>
After completion, create `/Users/kristiakarakatsani/Repos/finance-tracker/.planning/phases/11-react-scaffold-and-auth/11-04-SUMMARY.md`
</output>
