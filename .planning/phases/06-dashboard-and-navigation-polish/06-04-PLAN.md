---
phase: 06-dashboard-and-navigation-polish
plan: "04"
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - NAV-01
must_haves:
  truths:
    - "All five phase requirements are visually verified in the running app"
    - "Dashboard communicates financial position clearly with visual hierarchy"
    - "Sidebar reliably shows which page is active without orange"
  artifacts:
    - path: "frontend/pages/dashboard.py"
      provides: "Working dashboard with styled cards and charts"
    - path: "frontend/main.py"
      provides: "Working sidebar with left border active indicator"
  key_links:
    - from: "running app at localhost:8501"
      to: "dashboard page"
      via: "visual inspection of metric cards, charts, sidebar"
      pattern: "n/a — manual verification"
---

<objective>
Visual verification checkpoint: confirm all five Phase 6 requirements are met in the running app before marking the phase complete.

Purpose: DASH-01, DASH-04, and NAV-01 are CSS/visual changes with no automated test coverage — human eyes must confirm they look correct.
Output: Confirmed approval or issues list to fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-and-navigation-polish/06-CONTEXT.md
@.planning/phases/06-dashboard-and-navigation-polish/06-02-SUMMARY.md
@.planning/phases/06-dashboard-and-navigation-polish/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the app and run automated pre-check</name>
  <files></files>
  <action>
Run the final automated checks before handing off to the user for visual inspection:

```bash
# Confirm no st.metric() calls remain in dashboard.py
grep -n "st\.metric" frontend/pages/dashboard.py
# Expected: no matches

# Confirm yaxis_tickformat bug is gone
grep -n "yaxis_tickformat" frontend/pages/dashboard.py
# Expected: no matches

# Confirm stacked bar is in place
grep -n "barmode" frontend/pages/dashboard.py
# Expected: at least one match with "stack"

# Confirm shadow CSS is present
grep -n "stPlotlyChart" frontend/main.py
# Expected: at least one match

# Confirm orange active button CSS is gone
grep -n "d97757" frontend/main.py
# Expected: no matches

# Run full test suite
pytest tests/ -q

# Start app (user will open browser)
python -m streamlit run frontend/main.py
```

If any grep check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/finance-tracker && pytest tests/ -q && ! grep -q "st\.metric\|yaxis_tickformat\|d97757" frontend/pages/dashboard.py frontend/main.py</automated>
  </verify>
  <done>All automated checks pass; app is running and accessible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All five Phase 6 requirements implemented:
- DASH-01: Net Worth (blue), Assets (green), Liabilities (red), Pension (grey) metric cards as styled HTML divs with pastel backgrounds, rounded corners, and drop shadows — replacing plain st.metric() widgets
- DASH-02: Net Worth delta shown in red when negative, green when positive
- DASH-03: Line chart y-axis labels use £ prefix and thousands comma separator (e.g. "£1,250" not "£1250")
- DASH-04: All Plotly chart containers have a subtle box-shadow; pension chart is now a single stacked bar with one segment per provider (narrower bars, legend showing provider names)
- NAV-01: Active sidebar page has a dark left border accent instead of an orange filled background; inactive pages have no border
  </what-built>
  <how-to-verify>
1. Open http://localhost:8501 in your browser (or the running app URL)
2. Navigate to the Dashboard page

**DASH-01 — Metric cards:**
- Confirm you see four cards (Net Worth, Total Assets, Total Liabilities, Total Pension)
- Each card should have a soft colored background: Net Worth=blue tint, Assets=green tint, Liabilities=red tint, Pension=grey tint
- Cards should have rounded corners and a subtle shadow
- Cards should NOT look like default Streamlit st.metric() widgets (no grey arrow delta indicators)

**DASH-02 — Negative delta color:**
- If your Net Worth dropped since the last snapshot: the delta shown on the Net Worth card should be RED
- If it increased: delta should be GREEN
- (If there is only one snapshot, no delta is shown — this is expected)

**DASH-03 — Line chart y-axis format:**
- Look at the "Amount over time" line chart
- Y-axis labels should show £ prefix with comma separators (e.g. "£10,000" or "£1,250")
- NOT "£10000" (no comma) or "10000" (no £)

**DASH-04 — Chart drop shadows + pension stacked bar:**
- All charts (line chart, pie charts, pension chart) should have a subtle shadow around their container
- The Pension Breakdown section (if you have pension accounts) should show ONE stacked bar labeled "Pension" with colored segments per provider and a legend
- The bar should be noticeably narrower than the full chart width

**NAV-01 — Sidebar active indicator:**
- Click through each page (Dashboard, Accounts, Liabilities, Pension, History, Configure)
- The currently active page button should show a dark left border on the left side of the button
- The active button should NOT have an orange background fill
- Inactive page buttons should look plain (no border, no background)
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe any issues found (e.g. "DASH-01 cards are missing background color")</resume-signal>
</task>

</tasks>

<verification>
User approves all five visual checks.
</verification>

<success_criteria>
- All automated grep checks pass (no st.metric, no old tick format bug, no orange CSS)
- Full test suite passes
- User confirms all five requirements (DASH-01 through DASH-04, NAV-01) are visually correct
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-and-navigation-polish/06-04-SUMMARY.md`
</output>
